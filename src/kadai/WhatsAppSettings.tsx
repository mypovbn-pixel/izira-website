import { FormEvent, useEffect, useMemo, useState } from "react";
import { Link, useRouter } from "./nav";
import { createClient, supabaseUrl } from "./supabase";
type Business={id:string;name:string;plan:string};type Connection={id:string;status:string;waba_id:string|null;phone_number_id:string|null;display_phone_number:string|null;connected_at:string|null;last_error:string|null};type Automation={id:string;event_key:string;enabled:boolean;template_name:string|null;language_code:string};type Message={id:string;event_key:string|null;recipient:string;status:string;template_name:string|null;created_at:string;error_message:string|null};const eventLabels:Record<string,string>={order_confirmed:"Order confirmed",order_ready:"Order ready",appointment_confirmed:"Appointment confirmed",appointment_reminder:"Appointment reminder",transport_picked_up:"Transport picked up",transport_dropped_off:"Transport dropped off",runner_collected:"Runner collected",runner_delivered:"Runner delivered"};const events=Object.keys(eventLabels);
export default function WhatsAppSettings(){const router=useRouter();const supabase=createClient();const [business,setBusiness]=useState<Business|null>(null);const [connection,setConnection]=useState<Connection|null>(null);const [automations,setAutomations]=useState<Automation[]>([]);const [messages,setMessages]=useState<Message[]>([]);const [loading,setLoading]=useState(true);const [notice,setNotice]=useState("");const [wabaId,setWabaId]=useState("");const [phoneNumberId,setPhoneNumberId]=useState("");const [accessToken,setAccessToken]=useState("");const pro=business?['pro','business'].includes(business.plan):false;async function load(){setLoading(true);const {data:{user}}=await supabase.auth.getUser();if(!user){router.replace('/login');return}const {data:b}=await supabase.from('businesses').select('id,name,plan').eq('owner_id',user.id).order('created_at').limit(1).maybeSingle();if(!b){router.replace('/onboarding');return}setBusiness(b as Business);const [{data:c},{data:a},{data:m}]=await Promise.all([supabase.from('whatsapp_connections').select('id,status,waba_id,phone_number_id,display_phone_number,connected_at,last_error').eq('business_id',b.id).maybeSingle(),supabase.from('whatsapp_automations').select('id,event_key,enabled,template_name,language_code').eq('business_id',b.id).order('event_key'),supabase.from('whatsapp_messages').select('id,event_key,recipient,status,template_name,created_at,error_message').eq('business_id',b.id).order('created_at',{ascending:false}).limit(12)]);setConnection(c as Connection|null);setAutomations((a||[]) as Automation[]);setMessages((m||[]) as Message[]);if(c){setWabaId(c.waba_id||'');setPhoneNumberId(c.phone_number_id||'')}setLoading(false)}useEffect(()=>{load()},[]);const automationMap=useMemo(()=>new Map(automations.map(a=>[a.event_key,a])),[automations]);async function connect(e:FormEvent){e.preventDefault();if(!pro){setNotice('Official WhatsApp automation is included with KADAI Pro.');return}const {data,error}=await supabase.functions.invoke('connect-whatsapp',{body:{waba_id:wabaId.trim(),phone_number_id:phoneNumberId.trim(),access_token:accessToken.trim()}});if(error){setNotice(error.message);return}if((data as any)?.error){setNotice((data as any).error);return}setAccessToken('');setNotice('Connected.');await load()}async function saveAutomation(eventKey:string,changes:Partial<Automation>){if(!business)return;const current=automationMap.get(eventKey);const row={business_id:business.id,event_key:eventKey,enabled:changes.enabled??current?.enabled??false,template_name:changes.template_name??current?.template_name??'kadai_transaction_update',language_code:changes.language_code??current?.language_code??'en'};const {error}=await supabase.from('whatsapp_automations').upsert(row,{onConflict:'business_id,event_key'});setNotice(error?error.message:'WhatsApp automation saved.');if(!error)await load()}if(loading)return <main className="shell"><div className="card">Opening WhatsApp settings…</div></main>;if(!business)return null;return <main className="shell"><div className="nav"><div><div className="eyebrow">KADAI · {business.name}</div><h1>WhatsApp Business</h1></div><Link className="btn secondary" href="/dashboard">← Dashboard</Link></div>{notice&&<div className="card">{notice}</div>}{!pro&&<div className="card"><div className="eyebrow">PRO FEATURE</div><h2>Official WhatsApp automation</h2><p className="muted">Free and Starter keep manual WhatsApp buttons. Meta Cloud API automation, including school-run pickup/drop-off updates, is reserved for Pro.</p><Link className="btn" href="/dashboard/plan">View Pro</Link></div>}<section className="section"><div className="grid3"><form className="card" onSubmit={connect}><h2>Meta Cloud API</h2><div className="field"><label>WhatsApp Business Account ID</label><input value={wabaId} onChange={e=>setWabaId(e.target.value)} disabled={!pro}/></div><div className="field"><label>Phone number ID</label><input value={phoneNumberId} onChange={e=>setPhoneNumberId(e.target.value)} disabled={!pro}/></div><div className="field"><label>Access token</label><input type="password" value={accessToken} onChange={e=>setAccessToken(e.target.value)} disabled={!pro}/></div><button className="btn" disabled={!pro}>{connection?.status==='connected'?'Reconnect WhatsApp':'Connect WhatsApp'}</button><p className="muted">Token is sent to the secure Supabase Edge Function and stored in Vault.</p></form><div className="card" style={{gridColumn:"span 2"}}><div className="eyebrow">STATUS</div><h2>{connection?.status==='connected'?'Connected':'Not connected'}</h2>{connection?.display_phone_number&&<p><b>{connection.display_phone_number}</b></p>}<div className="eyebrow">META WEBHOOK</div><code style={{overflowWrap:'anywhere'}}>{supabaseUrl}/functions/v1/whatsapp-webhook</code></div></div></section><section className="section"><h2>Automations</h2><div style={{display:'grid',gap:10}}>{events.map(key=>{const a=automationMap.get(key);return <div className="card" key={key} style={{display:'grid',gridTemplateColumns:'1.3fr .9fr .45fr',gap:12,alignItems:'center'}}><div><b>{eventLabels[key]}</b></div><input value={a?.template_name||'kadai_transaction_update'} onChange={e=>saveAutomation(key,{template_name:e.target.value})} disabled={!pro}/><label><input type="checkbox" checked={a?.enabled||false} onChange={e=>saveAutomation(key,{enabled:e.target.checked})} disabled={!pro}/> Auto</label></div>})}</div></section><section className="section"><h2>Recent WhatsApp messages</h2>{messages.map(m=><div className="card" key={m.id}><b>{m.event_key?eventLabels[m.event_key]||m.event_key:'WhatsApp message'}</b><p className="muted">{m.recipient} · {m.status} · {new Date(m.created_at).toLocaleString()}</p></div>)}</section></main>}
