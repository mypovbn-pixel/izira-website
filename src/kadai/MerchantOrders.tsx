import { useEffect, useState } from "react";
import { Link, useRouter } from "./nav";
import { createClient } from "./supabase";
type Order={id:string;order_number:number;customer_name:string;customer_phone:string;total:number|string;fulfilment:string;payment_status:string;order_status:string;created_at:string};type Receipt={id:string;order_id:string;storage_path:string;uploaded_at:string};const orderStates=["new","confirmed","preparing","ready","completed","cancelled"];const paymentStates=["awaiting_payment","receipt_uploaded","paid","rejected"];
export default function MerchantOrders(){const router=useRouter();const supabase=createClient();const [business,setBusiness]=useState<{id:string;name:string;slug:string}|null>(null);const [orders,setOrders]=useState<Order[]>([]);const [receipts,setReceipts]=useState<Receipt[]>([]);const [loading,setLoading]=useState(true);const [message,setMessage]=useState("");async function load(){const {data:{user}}=await supabase.auth.getUser();if(!user){router.replace("/login");return}const {data:b}=await supabase.from("businesses").select("id,name,slug").eq("owner_id",user.id).limit(1).maybeSingle();if(!b){router.replace("/onboarding");return}setBusiness(b);const [{data:o},{data:r}]=await Promise.all([supabase.from("orders").select("id,order_number,customer_name,customer_phone,total,fulfilment,payment_status,order_status,created_at").eq("business_id",b.id).order("created_at",{ascending:false}),supabase.from("payment_receipts").select("id,order_id,storage_path,uploaded_at").eq("business_id",b.id).order("uploaded_at",{ascending:false})]);setOrders((o||[]) as Order[]);setReceipts((r||[]) as Receipt[]);setLoading(false)}useEffect(()=>{load()},[]);async function updateOrder(id:string,field:"order_status"|"payment_status",value:string){const {error}=await supabase.from("orders").update({[field]:value}).eq("id",id);if(error){setMessage(error.message);return}await load()}async function openReceipt(orderId:string){const receipt=receipts.find(r=>r.order_id===orderId);if(!receipt){setMessage("No receipt attached to this order.");return}const {data,error}=await supabase.storage.from("payment-receipts").createSignedUrl(receipt.storage_path,120);if(error||!data?.signedUrl){setMessage(error?.message||"Unable to open receipt.");return}window.open(data.signedUrl,"_blank","noopener,noreferrer")}if(loading)return <main className="shell"><div className="card" style={{marginTop:60}}>Loading orders…</div></main>;if(!business)return null;return <main className="shell"><div className="nav"><div><div className="eyebrow">{business.name}</div><h1>Orders</h1></div><div className="navlinks"><Link href="/dashboard">Dashboard</Link><Link className="pill" href={`/${business.slug}`}>Storefront</Link></div></div>{message&&<div className="card">{message}</div>}<div style={{display:"grid",gap:12}}>{orders.map(o=>{const receipt=receipts.find(r=>r.order_id===o.id);return <article className="card" key={o.id}><div style={{display:"flex",justifyContent:"space-between",gap:18,flexWrap:"wrap"}}><div><div className="eyebrow">ORDER #{o.order_number}</div><h3>{o.customer_name}</h3><div className="muted">{o.customer_phone} · {o.fulfilment} · BND {Number(o.total).toFixed(2)}</div></div>{receipt&&<button className="btn" onClick={()=>openReceipt(o.id)}>View receipt</button>}</div><div style={{display:"grid",gridTemplateColumns:"1fr 1fr",gap:12,marginTop:18}}><div className="field"><label>Payment</label><select value={o.payment_status} onChange={e=>updateOrder(o.id,"payment_status",e.target.value)}>{paymentStates.map(s=><option key={s}>{s}</option>)}</select></div><div className="field"><label>Order</label><select value={o.order_status} onChange={e=>updateOrder(o.id,"order_status",e.target.value)}>{orderStates.map(s=><option key={s}>{s}</option>)}</select></div></div></article>})}</div></main>}
